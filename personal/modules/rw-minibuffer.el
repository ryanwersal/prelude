;;; rw-minibuffer.el --- minibuffer configuration

;;; Commentary:

;;; minibuffer configuration

;;; Code:

(define-key minibuffer-local-map (kbd "C-c e") 'prelude-eval-and-replace)

(provide 'rw-minibuffer)

;;; rw-minibuffer.el ends here
